import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
} from "./button.svelte";

export {
	//
	Root as Button,
	buttonVariants,
	Root,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant,
	type ButtonProps as Props,
};
